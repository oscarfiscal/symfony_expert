{{
    form_start(form_busqueda, {'attr': {'novalidate': 'novalidate','class': 'form-inline'},
    method: 'POST',
    action: path('app_search')
    })
    }}
{{
    form_widget(form_busqueda.busqueda, {'attr': {'class': 'form-control mr-sm-2', 'placeholder': 'Buscar', 'aria-label': 'Search'}})
    }}
  

{{
    form_widget(
    form_busqueda.buscar,
    {
        attr:{
            class:'btn btn-primary ml-2',
        }
    }
    )
}}

{{form_errors(form_busqueda.busqueda)}}
    
    {{ form_end(form_busqueda)
}}