{{
    form_start(form_search, {'attr': {'novalidate': 'novalidate','class': 'form-inline'},
    method: 'POST',})
    }}
    {{ form_widget(form_search.search) }}
    {{ form_widget(form_search.submit) }}
    {{ form_end(form_search)
}}