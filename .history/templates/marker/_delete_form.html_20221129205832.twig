<form method="post" action="{{ path('app_marker_delete', {'id': marker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
   <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marker.id) }}">
    <button class="btn btn-link mg-3">
     <span class="oi oi-delete" title="Eliminar" aria-hidden="true"></span>
     </button>

     <a href="{{ path('app_marker_delete',{id:marker.id}  )}}" class="btn btn-link mg-3">
              <span  title="Eliminar" aria-hidden="true"></span>
            </a>
</form>
